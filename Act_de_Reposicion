Alch no sé cómo se usa esta madre, pero se hace lo que se puede
Code: 
loop,  load it
	   Subt ten
       Skipcond 400 / have we completed the multiplication?
       jump normal
       jump first 
	  
normal,load Y /print y
	   Output
first, load X /print x
       Output
      
       load Y /y += x
	   add X
       Store Y
      
	   load X /x += y
       add Y
       Store X
      
       Load it /iteración --
       Subt one
       Store it

       Skipcond 400 / have we completed the multiplication?
       Jump loop / no; repeat loop
       / yes, so exit the loop
      
Halt

X, DEC 1
Y, DEC 0
it, DEC 10
one, DEC 1
ten, DEC 10
